
<%= simple_form_for(@nature) do |f| %>
	<%= error_messages_for(@nature) %>
  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :ativo %>
  </div>

  <div class="form-actions">
<%= btn_save %>
<%= btn_cancel(natures_path) %>
  </div>
<% end %>