
<%= simple_form_for(@server) do |f| %>
	<%= error_messages_for(@server) %>
  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :registration %>
    <%= f.input :office, collection: @offices %>
    <%= f.input :active %>
  </div>

  <div class="form-actions">
<%= btn_save %>
<%= btn_cancel(servers_path) %>
  </div>
<% end %>